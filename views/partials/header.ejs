<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'WanderLust - Travel Journal' %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/social.css">
    
    <!-- Favicon -->
    <link rel="icon" href="/images/favicon.ico">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Additional Meta Tags -->
    <meta name="description" content="<%= description || 'Share your travel experiences with the WanderLust community.' %>">
    <meta name="keywords" content="travel, journal, wanderlust, adventure, explore">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= typeof currentUrl !== 'undefined' ? currentUrl : 'https://wanderlust.example.com' %>">
    <meta property="og:title" content="<%= typeof title !== 'undefined' ? title : 'WanderLust - Travel Journal' %>">
    <meta property="og:description" content="<%= typeof description !== 'undefined' ? description : 'Share your travel experiences with the WanderLust community.' %>">
    <% if (typeof imageUrl !== 'undefined') { %>
    <meta property="og:image" content="<%= imageUrl %>">
    <% } else { %>
    <meta property="og:image" content="/images/og-image.jpg">
    <% } %>
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="<%= typeof currentUrl !== 'undefined' ? currentUrl : 'https://wanderlust.example.com' %>">
    <meta property="twitter:title" content="<%= typeof title !== 'undefined' ? title : 'WanderLust - Travel Journal' %>">
    <meta property="twitter:description" content="<%= typeof description !== 'undefined' ? description : 'Share your travel experiences with the WanderLust community.' %>">
    <% if (typeof imageUrl !== 'undefined') { %>
    <meta property="twitter:image" content="<%= imageUrl %>">
    <% } else { %>
    <meta property="twitter:image" content="/images/og-image.jpg">
    <% } %>
    
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8f9fa;
        }
        .card {
            border: none;
            border-radius: 15px;
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }
        .card-header {
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            border-bottom: none;
        }
        .btn-primary {
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            border: none;
            padding: 10px 25px;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(37, 117, 252, 0.3);
        }
    </style>
</head>
<body>
    <!-- Navigation is included via navbar.ejs -->
    <%- include('../includes/navbar') %>
    
    <!-- Flash Messages -->
    <% if(success && success.length > 0) { %>
        <div class="alert alert-success alert-dismissible fade show m-3" role="alert">
            <%= success %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } %>
    
    <% if(error && error.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show m-3" role="alert">
            <%= error %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } %>
